# Kyo-Pre! Site 運用マニュアル

このドキュメントでは、エンジニア以外のメンバーがサイトのコンテンツを更新・管理する方法について解説します。

---

## 1. システムの仕組み（概要）

このサイト（Kyo-Pre! Site）は、サーバー上のデータベースを持たず、**ファイル（JSONや画像、テキスト）を読み込んで表示する**仕組みになっています。
つまり、以下の特定のファイルを書き換えてGitHubにアップロード（Push）するだけで、サイトの内容が更新されます。

**基本ルール:**
- データファイルは全て `public/data/` フォルダの中にあります。
- 更新が反映されるまで、アップロード後 1〜5分程度かかります。

---

## 2. 各エリアの更新手順

### A. Tier List（左上の大きい画像）

**【推奨】Botを使用する方法**
Discord Botのコマンド `/update_tier` を使用して、画像をアップロードしてください。
自動的にファイル名が変換され、サイトに反映されます。対応形式は `jpg` または `png` です。

**【緊急時】手動で更新する方法**
1. 新しいTier表の画像を用意します。
2. ファイル名を `tier_list.jpg` に変更します。
3. `public/data/` フォルダの中にある既存の `tier_list.jpg` を、新しい画像で上書き保存します。
4. GitHubへ変更をアップロードします。

---

### B. YouTube / Note（右上・右下のカード）

これらは**Botが自動で更新**するため、基本的には手動での作業は不要です。
もし表示がおかしい場合や、緊急で別の記事を表示したい場合は以下のファイルを編集します。

- **YouTube**: `public/data/youtube_latest.json`
- **Note**: `public/data/note_latest.json`

ファイルの中身は以下のようになっています。上書きすればその内容が表示されます。
```json
{
  "items": [
    {
      "title": "記事や動画のタイトル",
      "link": "https://...",
      "pubDate": "2025-01-01",
      "thumbnail": "画像のURL"
    }
  ]
}
```

---

### C. Tech / Guide（中央下の記事カード）

新しい攻略記事やレポートを掲載する場合の手順です。

**ステップ 1: 記事ファイルの作成**
1. 記事の本文をMarkdown形式（`.md`ファイル）で作成します。
   - 一般的なブログと同じように書けます。HTMLタグは不要です。
   - ファイル名には日付や内容を含めると管理しやすいです（例: `20251229_red_apollo_guide.md`）。
2. 作成したファイルを `public/data/guides/` フォルダに保存します。

**ステップ 2: 目録（JSON）の更新**
1. `public/data/guides.json` を開きます。
2. `items` リストの**一番上**に、新しい記事の情報を追記します。ここがサイト上のカード表示に使われます。

```json
{
    "items": [
        {
            "title": "ここに記事タイトル",
            "date": "2025-01-01",
            "category": "GUIDE",
            "path": "/data/guides/20251229_red_apollo_guide.md", 
            "summary": "カードに表示される短い要約文をここに書きます。"
        },
        ...（以前の記事）
    ]
}
```
※ `path` は `/data/guides/ファイル名.md` の形式で指定してください。

---

### D. Meta Matrix（メインの相性表）

**【推奨】Botを使用する方法**
Discord Botのコマンドを使用するのが最も安全で簡単です。 Botを操作すると、自動的にサイトのデータも更新されます。
- **デッキ追加**: `/add_deck` コマンド（画像添付必須）
- **相性入力**: `/matchup` → ダッシュボードから入力
- **デッキ削除**: `/matchup` → ダッシュボードから「デッキ削除」

**【緊急時】手動で修正する方法**
Botが動かない場合などは、`public/data/matrix.json` を直接編集して修正可能です。

1. **decks（デッキ情報）**:
   デッキの名前画像パスを管理しています。
   ```json
   {
     "id": "デッキID（英語推奨）",
     "name": "デッキ名（日本語可）",
     "img": "/data/archives/.../画像ファイル.jpg"
   }
   ```

2. **winRates（勝率情報）**:
   それぞれの対面ごとの勝率（％）を記述します。
   ```json
   "デッキAの名前": {
       "デッキBの名前": 60  // デッキAが60%有利
   }
   ```
   ※ 間違えやすいため、極力Botの使用を推奨します。

---

## 3. よくあるミスと対策

- **サイトが真っ白になった / 更新されない**
  - **原因**: JSONファイルの「カンマ（,）」忘れや「閉じ括弧（} ]）」の不足が考えられます。
  - **対策**: 編集したJSONファイルを [JSONLint](https://jsonlint.com/) などのチェックツールに貼り付けて、エラーがないか確認してください。

- **画像が表示されない**
  - **原因**: ファイルパスが間違っているか、画像ファイル自体がアップロードされていません。
  - **対策**: `public/data/...` に画像があるか、ファイル名が大文字・小文字まで一致しているか確認してください。
